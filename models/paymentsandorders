with orders as (

    select
        *
    from  {{ref('stg_orders')}}

),

payments as (

    select
        *
    from  {{ref('stg_payments')}}

select order_id,customer_id,* from Payments P inner join orders on p.order_id=or.order_id